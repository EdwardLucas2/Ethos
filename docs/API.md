# API

Javalin backend. OpenAPI spec generated at compile time from `@OpenApi` annotations; served at `GET /openapi.json`. Frontend types and hooks are generated by Orval — never define API types by hand.

## Conventions

### Authentication

Two before-handlers:

- **`requireAuth`** — verifies the SuperTokens JWT and loads the `users` row by `supertokens_user_id`. Attaches `userId` (our UUID) to the context. Used on all protected routes except registration.
- **`requireJwt`** — verifies the SuperTokens JWT only. Does **not** load the `users` row. Used only on `POST /users` (the user row does not exist yet at that point).

The `userId` on the context is always our `users.id` UUID — never the SuperTokens user ID, and never a caller-supplied value.

### Error responses

All errors return:

```json
{ "error": "Human-readable message" }
```

Mapped globally via `app.exception()` — no per-handler error handling.

### File URLs

`avatarUrl` and `photoUrl` fields are resolved URLs, not raw IDs. The backend resolves them via `FileStorageService` before returning. Callers never construct storage keys.

### IDs

All IDs are UUIDs. `cycleNumber` is the integer sequence number (`cycles.cycle_number`), not a UUID.

---

## Users

### `POST /users` — Register

Creates the `users` row after SuperTokens has created the auth account. Called once per signup.

**Auth:** `requireJwt` (JWT verified; no `users` row lookup)

**Request body:**

```json
{ "displayName": "Edward Lucas" }
```

**Response `201`:**

```json
{
    "id": "3fa85f64-...",
    "displayName": "Edward Lucas",
    "tag": "edward4f2a",
    "email": "edward@example.com",
    "avatarUrl": null
}
```

Backend derives `supertokens_user_id` and `email` from the JWT. Tag is auto-generated by the service (first word of display name, lowercased, stripped to alphanumeric, truncated to 8 chars, plus 4 random alphanumeric chars; retries on collision).

**Errors:**

- `409` — a `users` row already exists for this SuperTokens account

---

### `GET /users/me` — Get current user

**Auth:** `requireAuth`

**Response `200`:**

```json
{
    "id": "3fa85f64-...",
    "displayName": "Edward Lucas",
    "tag": "edward4f2a",
    "email": "edward@example.com",
    "avatarUrl": null
}
```

---

### `PATCH /users/me` — Update profile

**Auth:** `requireAuth`

**Request body** (all fields optional; at least one required):

```json
{ "displayName": "Ed Lucas" }
```

MVP: `displayName` only. Avatar upload is out of scope.

**Response `200`:** updated user (same shape as `GET /users/me`).

**Errors:**

- `400` — body is empty or `displayName` is blank

---

### `GET /users/search?tag={prefix}` — Search users by tag

Used in the contract builder invite flow and Friends screen. Prefix-matches against `users.tag`.

**Auth:** `requireAuth`

**Query params:**

| Param | Required | Description                                 |
| ----- | -------- | ------------------------------------------- |
| `tag` | Yes      | Tag prefix to search. Minimum 2 characters. |

**Response `200`:**

```json
[
    {
        "id": "3fa85f64-...",
        "displayName": "Alex",
        "tag": "alex3b9c",
        "avatarUrl": null,
        "isContact": true
    }
]
```

- Excludes the calling user from results.
- `isContact` — `true` if this user is already in the caller's contacts.
- Returns up to 20 results, ordered by tag ascending.

**Errors:**

- `400` — `tag` param missing or fewer than 2 characters

---

## Contacts

_To be designed._

---

## Contracts

_To be designed._

---

## Participants

_To be designed._

---

## Dashboard

_To be designed._

---

## Cycles

_To be designed._

---

## Evidence

_To be designed._

---

## Votes

_To be designed._

---

## Resolutions

_To be designed._

---

## Pesters

_To be designed._

---

## Notifications

_To be designed._

---

## Device Tokens

_To be designed._
