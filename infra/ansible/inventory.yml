all:
  hosts:
    runner:
      # Update ansible_host with the IP from: cd infra/tofu/runner && tofu output runner_public_ip
      ansible_host: "REPLACE_WITH_TOFU_OUTPUT"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
