appId: com.ethos.app
env:
    TEST_PASSWORD: TestPassword123!
---
# Setup: create a fresh account via sign-up so login credentials are known
- runScript:
      file: gen-test-email.js
- launchApp:
      clearState: true
- extendedWaitUntil:
      visible: 'WELCOME BACK.'
      timeout: 10000
- tapOn:
      id: 'signup-link'
- extendedWaitUntil:
      visible: 'CREATE ACCOUNT'
      timeout: 10000
- tapOn:
      id: 'email-input'
- inputText: ${output.TEST_SIGNUP_EMAIL}
- pressKey: enter
- tapOn:
      id: 'password-input'
- inputText: ${TEST_PASSWORD}
- tapOn:
      id: 'submit-button'
- extendedWaitUntil:
      visible: 'Welcome!'
      timeout: 10000

# Test: clear state (log out), then log in with the same credentials
- launchApp:
      clearState: true
- extendedWaitUntil:
      visible: 'WELCOME BACK.'
      timeout: 10000
- tapOn:
      id: 'email-input'
- inputText: ${output.TEST_SIGNUP_EMAIL}
- pressKey: enter
- tapOn:
      id: 'password-input'
- inputText: ${TEST_PASSWORD}
- tapOn:
      id: 'submit-button'
- assertVisible: 'Welcome!'
