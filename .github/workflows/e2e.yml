# Disabled: Android emulator E2E doesn't match the iOS-specific sign-up
# flow fix (Maestro "Use Strong Password?" sheet handling). Re-enable once
# the flow is made cross-platform.
#
# name: E2E
#
# on:
#     push:
#         branches: [main]
#         paths:
#             - "app/**"
#             - "docker/**"
#             - "backend/**"
#             - ".github/workflows/e2e.yml"
#             - ".github/actions/setup-maestro/**"
#     pull_request:
#         paths:
#             - "app/**"
#             - "docker/**"
#             - "backend/**"
#             - ".github/workflows/e2e.yml"
#             - ".github/actions/setup-maestro/**"
#     workflow_dispatch:
#
# jobs:
#     maestro:
#         runs-on: ubuntu-latest
#         timeout-minutes: 60
#         env:
#             # Single source of truth for both the cache key and setup-maestro's
#             # version input below — previously two independent hardcoded "2.5.1"
#             # literals that could silently drift apart on a version bump.
#             MAESTRO_VERSION: "2.5.1"
#         steps:
#             - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#               with:
#                   persist-credentials: false
#
#             - uses: ./.github/actions/setup-node-cached
#               with:
#                   cache-dependency-path: app/package-lock.json
#
#             - uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
#               with:
#                   java-version: "17"
#                   distribution: "temurin"
#
#             - name: Install app dependencies
#               working-directory: app
#               run: npm ci
#
#             # This step and --build below are no-ops on ubuntu-latest's ephemeral
#             # runners (nothing to clean, nothing to keep in sync) — they're
#             # leftovers from running on the self-hosted Oracle runner (infra/),
#             # which doesn't have the resources to run Maestro/Android-emulator
#             # jobs. Left in place (rather than deleted) since they're harmless
#             # here and become relevant again if a persistent runner is used.
#             - name: Clean up previous run
#               env:
#                   DATABASE_USER: ${{ secrets.DATABASE_USER }}
#                   DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
#               run: |
#                   pkill -f "expo start" || true
#                   pkill -f "metro" || true
#                   adb kill-server || true
#                   docker compose -f docker/docker-compose.dev.yml down -v --remove-orphans || true
#
#             - name: Start backend services
#               env:
#                   DATABASE_USER: ${{ secrets.DATABASE_USER }}
#                   DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
#               run: docker compose -f docker/docker-compose.dev.yml up -d --wait --build
#
#             - name: Show service logs on failure
#               if: failure()
#               env:
#                   DATABASE_USER: ${{ secrets.DATABASE_USER }}
#                   DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
#               run: docker compose -f docker/docker-compose.dev.yml logs postgres supertokens auth backend
#
#             - uses: ./.github/actions/setup-dbmate
#               with:
#                   version: "2.33.0"
#                   sha256: "dfd4027141b6e4357158099bdfe6c02b286af70f8e49057937744cf80b38ea1c"
#
#             - name: Migrate database
#               env:
#                   DATABASE_USER: ${{ secrets.DATABASE_USER }}
#                   DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
#               run: dbmate --url "postgresql://$DATABASE_USER:$DATABASE_PASSWORD@localhost:5432/ethos?sslmode=disable" --migrations-dir backend/db/migrations migrate
#
#             - name: Cache Maestro
#               id: maestro-cache
#               uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
#               with:
#                   path: ~/.maestro
#                   key: maestro-${{ env.MAESTRO_VERSION }}
#
#             - uses: ./.github/actions/setup-maestro
#               if: steps.maestro-cache.outputs.cache-hit != 'true'
#               with:
#                   version: ${{ env.MAESTRO_VERSION }}
#
#             - name: Add Maestro to PATH
#               if: steps.maestro-cache.outputs.cache-hit == 'true'
#               run: echo "$HOME/.maestro/bin" >> $GITHUB_PATH
#
#             - name: Cache APK
#               id: apk-cache
#               uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
#               with:
#                   path: app/android/app/build/outputs/apk/debug/app-debug.apk
#                   key: apk-${{ hashFiles('app/app.json', 'app/package-lock.json') }}
#
#             - name: Generate Android project
#               if: steps.apk-cache.outputs.cache-hit != 'true'
#               working-directory: app
#               run: npx expo prebuild --platform android --clean
#
#             - name: Cache Gradle
#               if: steps.apk-cache.outputs.cache-hit != 'true'
#               uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
#               with:
#                   path: |
#                       ~/.gradle/caches
#                       ~/.gradle/wrapper
#                   key: gradle-${{ hashFiles('app/android/**/*.gradle*', 'app/android/**/gradle-wrapper.properties') }}
#                   restore-keys: gradle-
#
#             - name: Build APK
#               if: steps.apk-cache.outputs.cache-hit != 'true'
#               working-directory: app/android
#               run: ./gradlew assembleDebug --no-daemon
#
#             - name: Enable KVM
#               run: |
#                   echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#                   sudo udevadm control --reload-rules
#                   sudo udevadm trigger --name-match=kvm
#
#             - name: Cache AVD
#               id: avd-cache
#               uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
#               with:
#                   path: ~/.android/avd/*
#                   # Includes target (google_apis) — bump this whenever the AVD
#                   # config below changes, or a stale cache hit silently keeps
#                   # serving an AVD built with the old config.
#                   key: avd-api34-google_apis-x86_64
#
#             - name: Create AVD snapshot
#               if: steps.avd-cache.outputs.cache-hit != 'true'
#               uses: reactivecircus/android-emulator-runner@e89f39f1abbbd05b1113a29cf4db69e7540cae5a # v2.37.0
#               with:
#                   api-level: 34
#                   arch: x86_64
#                   target: google_apis
#                   force-avd-creation: false
#                   emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
#                   disable-animations: false
#                   script: echo "AVD snapshot created"
#
#             - name: Run Maestro flows
#               uses: reactivecircus/android-emulator-runner@e89f39f1abbbd05b1113a29cf4db69e7540cae5a # v2.37.0
#               with:
#                   api-level: 34
#                   arch: x86_64
#                   target: google_apis
#                   force-avd-creation: false
#                   emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
#                   disable-animations: true
#                   script: |
#                       cd app && npx expo start &
#                       timeout 120 bash -c 'until curl -sf http://localhost:8081/status > /dev/null; do sleep 2; done'
#                       adb install app/android/app/build/outputs/apk/debug/app-debug.apk
#                       adb reverse tcp:8081 tcp:8081
#                       ./scripts/run-e2e.sh
